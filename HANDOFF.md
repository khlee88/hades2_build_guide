# 하데스 2 빌드 어드바이저 — 작업 핸드오프

> 모델 전환 시 이 문서를 먼저 읽는다. 각 단계의 산출물은 아래 명시한 파일에 남기고,
> 완료한 항목은 체크박스를 채운 뒤 `## 진행 로그`에 한 줄 기록한다.

## 프로젝트 목표

게임 중 폰 브라우저에서 열어 쓰는 **하데스 2 빌드 추천 웹앱** (APK 아님, 단일 HTML → Artifact 배포).

플레이 흐름:
1. 런 시작: 무기/아스펙트 선택 → 추천 아르카나 + 유물 + **열린 빌드 방향 여러 개**
2. 신 선택지 등장 → 후보 신 체크 → 어느 신을 고를지 + 이유
3. 은혜 선택지 등장 → 뜬 은혜 체크 → 우선순위 1/2/3 + 한 줄 이유
4. 선택 확정 → 보유 빌드에 누적 → 다음 추천은 누적 빌드 기반 (듀오/전설 조건 추적)

## 확정된 결정사항

- 대상 무기: **초반 4개** — 마녀의 지팡이 / 자매의 검 / 그림자 화염 / 월장석 도끼 (한글판 표기는 1단계에서 재확인)
- 표기: **한글 게임 명칭 기준**, 영어 병기
- 추천 기준: 커뮤니티 정석 · 초보자 빌드
- 빌드는 무기 선택 시 고정하지 않고, **첫 신 등장에 따라 유연하게** 방향을 제시
- 추천 로직: 규칙 기반 점수 계산 (AI 호출 없음). 애매한 경우 "Claude에게 묻기"는 옵션
- 밸런스 패치 반영은 후순위
- 런 상태는 브라우저 localStorage에 저장
- **은혜 칸은 5개(일반 공격/기술/마법/질주/마력)이며 칸당 신 1명** — 교체 시 기존 은혜 소멸
- **헤르메스·아르테미스 은혜는 칸을 차지하지 않음** (1-B에서 근거 기반 확정, ISSUES.md 참조)

## 역할 분담 원칙

| 모델 | 역할 |
|---|---|
| **페이블 (Fable)** | 판단·설계·리뷰·튜닝 — 스키마, 추천 규칙, 빌드 방향 정의, 결과 검토 |
| **오퍼스 (Opus)** | 명세가 정해진 뒤의 조사·구현·배포·검증 — 데이터 채우기, 코드 작성, 테스트 |

---

## 1단계 — 데이터

### 1-A. 스키마 · 조사 지시서 — 담당: 페이블
- [x] `data/SCHEMA.md` — 무기/아스펙트/신/은혜/듀오·전설 조건/아르카나/유물/헥스 데이터 구조 정의 (JSON 필드, 슬롯 enum, 조건 표현 방식)
- [x] `data/RESEARCH_GUIDE.md` — 조사 체크리스트, 출처 우선순위(위키 → 커뮤니티 가이드 → 스트리머 티어), 한글명 검증 방법
- [x] `data/BUILD_DIRECTIONS.md` — 초보 정석 빌드 방향 초안 (무기별 2~4개 방향, 각 방향의 핵심 신·슬롯)

### 1-B. 데이터 수집 — 담당: 오퍼스
- [x] `data/weapons.json` — 4무기 + 각 아스펙트(초기 해금 아스펙트 우선), 핵심 슬롯 특성
- [x] `data/gods.json` — 등장 신 전체 (아프로디테·아폴론·아레스·데메테르·헤파이스토스·헤라·헤스티아·포세이돈·제우스 + 헤르메스·셀레네·아르테미스·카오스·아라크네·이카로스·나르키소스·에코·메데이아·키르케 등 비선택 소스 포함 여부는 SCHEMA에 따름)
- [x] `data/boons.json` — 신별 은혜 전체: 슬롯, 효과 요약, 상태이상(저주) 부여 여부, 희귀도 스케일
- [x] `data/duo_legendary.json` — 듀오/전설 은혜 + **전제 조건** (필수 은혜 목록, OR/AND)
- [x] `data/arcana.json` — 아르카나 카드, 비용(grasp), 효과, 초보 추천 세트
- [x] `data/keepsakes.json` — 유물, 효과, 초반 추천
- [x] `data/hexes.json` — 셀레네 비술 (Hex)
- [x] `data/hammers.json` — 다이달로스 망치(무기 강화) 4무기 전체 + 상충 관계
- [x] `data/ISSUES.md` — 조사 중 불일치·누락·한글명 불확실 항목 목록

> **1-B 완료.** 은혜 148 / 융합·전설 47 / 무기 6 / 아르카나 25 / 기념품 33 / 비술 9 / 망치 58. `node data/validate.js` 오류 0건, 미검증 0%.
> **`data/ISSUES.md`의 [P1] 항목을 먼저 읽을 것** — 1-A 초안의 한글 용어가 대부분 오역이었고 SCHEMA.md를 갱신했음.

### 1-C. 데이터 리뷰 — 담당: 페이블
- [x] 듀오/전설 전제조건 오류 검수
- [x] 은혜 슬롯·시너지 태그 누락 검수
- [x] `BUILD_DIRECTIONS.md` 확정 (데이터 기반으로 수정) → 정본은 **`data/build_directions.json`** (엔진 로드용)
- [x] 사용자에게 한글 명칭 확인 요청

---

## 2단계 — 추천 엔진

> **1-C 완료.** 융합·전설 조건 전량 재대조(오류 0), 태그 재정의(duo_key/per_hit/pct_dmg/cooldown_burst), 상충 필드 통합, 빌드 방향을 `data/build_directions.json`으로 확정. 검증 규칙 13종 통과.
> 2-A는 `build_directions.json`의 principles/magick_rule과 SCHEMA의 "엔진이 반드시 지켜야 할 것"을 입력으로 삼는다.

### 2-A. 규칙 설계 — 담당: 페이블
- [x] `engine/DESIGN.md` — 점수 계산 규칙: 무기별 핵심 슬롯 가중치, 빌드 방향 후보 생성·수렴 방식, 듀오/전설 접근도 가산, 채워진 슬롯 감점, 신 선택 추천 로직
- [x] `engine/SCENARIOS.md` — 테스트 시나리오 (무기 × 첫 신 × 이후 선택 5~8개 케이스, 기대 결과)

> **2-A 완료.** DESIGN.md §10 모듈 계약대로 구현. 모든 숫자는 `engine/weights.js` 상수로 분리(2-C 튜닝용). SCENARIOS.md 16개를 `engine/test.js`로 자동 실행하고 결과를 TEST_RESULTS.md에 남긴다.

### 2-B. 구현 — 담당: 오퍼스
- [x] `engine/recommend.js` — 순수 함수 모듈 (브라우저·Node 양쪽 동작)
- [x] `engine/weights.js` — DESIGN.md의 모든 상수
- [x] `engine/test.js` — SCENARIOS.md 자동 실행, 추천 결과 출력
- [x] 실행 결과를 `engine/TEST_RESULTS.md`에 저장

> **2-B 완료.** `node engine/test.js` → **PASS 16 / FAIL 0**. 결과는 `engine/TEST_RESULTS.md`.
> 구현 중 발견해 고친 것 3건 + 시나리오 결함 1건은 아래 진행 로그와 SCENARIOS.md 주석 참조.

### 2-C. 튜닝 — 담당: 페이블
- [x] TEST_RESULTS 검토, 이상 추천 식별
- [x] 가중치·규칙 수정 → DESIGN.md §13 갱신 → 재실행 (페이블이 직접 적용, 22/22 PASS)

---

## 3단계 — 모바일 UI

> **2-C 완료.** 시나리오 16 → 22개, **PASS 22 / FAIL 0**. 설계 결함 3건(허수 융합 진전/손실, 은혜 단계 신 풀 항, 첫 신 보너스 순서 의존) + 구현 누락 1건(칸 점유 은혜 회피) 수정. 정본은 `engine/DESIGN.md §13`. 프로브 스크립트는 `engine/probe/`.

### 3-A. 화면 명세 — 담당: 페이블
- [x] `ui/SPEC.md` — 화면 흐름(런 시작 → 신 선택 → 은혜 선택 → 빌드 현황), 한 손 조작 기준, 각 화면 필수 요소, 상태 저장 시점

> **3-A 완료.** `ui/SPEC.md` — 6화면(런 시작/홈/신/은혜/망치/관리), 한 손·3초 원칙, 다크 고정, 저장 키 3종·되돌리기, 엔진 호출 지점, `app/build.js`로 단일 파일 생성, 완료 기준 7항.

### 3-B. 구현 · 배포 — 담당: 오퍼스
- [x] `app/build.js` + `app/src/` → `app/index.html` 단일 파일 생성 (데이터·엔진 인라인, 엔진 파일은 복사만)
- [x] localStorage 런 상태 저장/복구/초기화
- [x] Artifact 배포 → URL을 `README.md`에 기록
- [x] 폰 뷰포트(375px) 검증 스크린샷

---

## 4단계 — 검증 · 피드백

> **3-B 완료.** 배포: https://claude.ai/code/artifact/97ec8735-53e7-4442-8543-5d6f4ae71a02
> 완료 기준 7항 전부 통과. 이제 폰에서 열어 실제로 한 판 돌려보고 피드백을 주면 4-A.

### 4-A. 실플레이 피드백 반영 — 담당: 페이블
- [x] 1차 실플레이 피드백 (갤럭시 S26+ 크롬) 접수 — 5건. 페이블 담당 2건 처리: 아르카나 재설계(DESIGN §14), 이해도 1단위 확정
- [ ] 2차 피드백 (추천 품질·UI) — 재배포 후 대기

### 4-B. 유지보수 — 담당: 오퍼스

> **1차 피드백 UI 작업 (페이블이 명세, 오퍼스가 구현).** 엔진·데이터는 페이블이 이미 고쳤고 `node engine/test.js` 24/24, `node app/build.js`까지 끝났다. 아래 5건은 `app/src/*`만 건드린다. 엔진 파일 수정 금지.

- [x] **U1 글자 크기** — S26+ 크롬에서 글씨가 작아 손으로 확대함. 원인 미확정(아티팩트 셸이 iframe 폭을 데스크톱으로 잡는 듯). 두 겹으로 처리:
  (a) `html{font-size:clamp(15px, 4.4vw, 22px)}` 로 루트 크기를 뷰포트 비례로, `app.css`의 px 글자 크기를 전부 rem으로 변환(16px=1rem). 탭 타겟 최소 44px는 px 유지.
  (b) 관리 > 설정에 **글자 크기** 세그먼트(보통 100% / 크게 115% / 아주 크게 130%) → `PREFS.font_scale`, `#app{font-size: calc(1rem * var(--fs))}` 로 적용. 기본 100%.
- [x] **U2 이해도 스텝퍼** — 관리의 10/15/20/30 세그먼트 제거. **런 시작 화면 아르카나 카드 상단**에 `[−] 20 [+]` 스텝퍼(10~30, 1단위, `PREFS.grasp_cap`). 바꾸면 `E.recommendArcana`로 즉시 재계산. 관리 화면에도 같은 스텝퍼(동기).
  아르카나 카드 표시: `cards`(태그 칩 핵심/무기 방향/보조) → `awakened`(각성 — 무료, 초록) → `next_up`("이해도 +N이면", 회색 접힘) → `free_cards`(잠긴 0 이해도, 접힘). 헤더 "아르카나 (사용 18 / 상한 20, 남음 2)".
- [x] **U3 획득 경로** — 기념품 `(헤카테에게 넥타르)`, 비술 `(셀레네 · 밤마다 1개)`. `recommendRunStart`가 `giver_ko`를 준다. 관리 화면 기념품 추천 카드에도 동일.
- [x] **U4 은혜 선택 스크롤 구조** — 목록이 길어 순위를 보려면 끝까지 스크롤해야 함. 추천을 **트레이 안으로 올려 하단 고정**: 2개 이상 선택 시 트레이가 위로 펼쳐져(최대 화면 높이 45%, 내부 스크롤) 순위 1·2·3을 **컴팩트 행**(순위·칸 칩·이름·이유 1줄·[결정])으로 보임. 경고는 행 아래 빨간 1줄(첫 경고만, 나머지 "+N"). 트레이 헤더 탭으로 접기/펼치기. 목록은 그 뒤에서 계속 스크롤. 망치 선택도 동일 구조.
- [x] **U5 신 선택 테두리** — 미선택: 테두리 2px 신 색 45% 투명(`color-mix` 또는 rgba). 선택: 테두리 3px 신 색 불투명 + `box-shadow:0 0 0 3px <신색 30%>` 외곽 글로우 + 배경 신 색 12% 틴트. 금색 inset 제거. 선택 순서 숫자(①②③) 우상단 작게.
- [x] 완료 후 `node app/build.js` → `node engine/test.js` 통과 확인 → 375×812 스크린샷(런 시작·은혜 선택 트레이 펼침·신 선택) → 커밋 메시지 작성(푸시는 사용자)

---

## 폴더 구조 (예정)

```
hades2_bulid/
├── HANDOFF.md          ← 이 문서
├── README.md           ← 배포 URL, 사용법
├── data/               ← 1단계 산출물
├── engine/             ← 2단계 산출물
├── ui/                 ← 3단계 명세
└── app/                ← 배포용 HTML
```

## 진행 로그

- 2026-09-09 — 프로젝트 시작, 핸드오프 작성 (페이블)
- 2026-09-09 — 1-A 완료: SCHEMA.md / RESEARCH_GUIDE.md / BUILD_DIRECTIONS.md(초안) 작성 (페이블). 다음: 1-B 오퍼스
- 2026-09-09 — 1-B 완료 (오퍼스): 하데스 위키(영문)로 은혜·전제조건 전량 수집, 나무위키로 한글판 명칭 전량 대조. data/*.json 7종 + merge.js/validate.js 작성, 검증 오류 0. SCHEMA.md 저주표·용어 대조표 갱신. 다음: 1-C 페이블 리뷰 (ISSUES.md [P1] 우선)
- 2026-09-09 — 1-B 보강 (오퍼스): 다이달로스 망치 58개 추가 수집 (`data/hammers.json`). 은혜와 동일한 3택1 선택 구조이므로 추천 대상에 포함해야 한다는 사용자 지적 반영. 상충 표현용 `conflicts`/`boon_conflicts` 필드 신설, SCHEMA·validate 확장. 검증 오류 0
- 2026-09-09 — 1-B 보강 2 (오퍼스): 슬롯 배타성(5칸 = 신 1명) 규칙을 사용자 확인 후 데이터에 명시. 헤르메스·아르테미스는 칸 미점유로 정정, 점유 은혜 45개에 conflicts 자동 생성(derive_slots.py), validate에 배타성 검사 4종 추가. 오류 0
- 2026-09-09 — 헤르메스 슬롯 점유 여부: 인게임 확인 불가로 근거 3종(질주 은혜 2개 보유 / 전제조건이 "고리"로 한정됨 / 융합 은혜 없음)에 따라 **칸 미점유로 확정**. 되돌리려면 derive_slots.py의 NON_SLOT_GODS 한 줄만 수정 (오퍼스)
- 2026-09-09 — 1-C 완료 (페이블): 융합 37+전설 10 전제조건 위키 재대조 오류 0. duo_gateway 폐기→duo_key 자동생성, beginner_safe 37개 엄선, per_hit/pct_dmg/cooldown_burst 신설, 마법 변형 계열 등 수동 상충 데이터화·conflicts 통합, power_tier 재조정. **`data/build_directions.json` 확정**(4무기 11방향 + 첫 신 매핑 + 생존 키트) 및 검증 규칙 추가(고의 오류 5종 주입 테스트 통과). 다음: 2-A 페이블
- 2026-09-09 — 2-A 완료 (페이블): engine/DESIGN.md(방향 적합도→가중치→후보 점수 모델, 교체·융합 손실·상충·신 풀·망치·기념품 규칙, 모듈 계약, 이유 템플릿) + engine/SCENARIOS.md(16개, 순위·배지·경고만 검사). 1-C 열린 질문 3건 결정. 신 풀 상한은 상수 GOD_POOL_CAP=4로 두고 미확인 표기. 다음: 2-B 오퍼스
- 2026-09-09 — 2-B 완료 (오퍼스): engine/{weights,recommend,test}.js 구현, 16/16 PASS. 구현 중 **버그 3건 수정** — (1) 0 이해도 아르카나를 각성 조건 무시하고 자동 편성 (2) 융합 진전/완성 판정에서 교체로 사라지는 은혜를 빼지 않아 "조건 완성"과 "무효화"가 동시 표시 (3) 융합 무효화 경고가 최대 7줄까지 나와 폰 화면 가독성 붕괴 → 목표 융합 우선 2건 + 건수 요약. **시나리오 결함 1건**: S06의 선택지가 변수를 분리하지 못해(융합 완성 후보·빈 칸 1순위 혼입) 반전이 순위에 안 드러남 → 칸 미점유 은혜만 제시하도록 교체, 엔진 로직은 불변
- 2026-09-09 — 2-C 완료 (페이블): TEST_RESULTS 점수 분포 + 프로브 6종 검토. **설계 결함** (1) 융합 진전/손실을 도달 불가 융합까지 세어 첫 은혜에 +6 허수·헤라 은혜 교체에 −12 (2) 은혜 단계 신 풀 감점은 이미 문을 고른 뒤라 무의미 (3) 첫 신 보너스가 first_god_map 나열 순서에 의존. **구현 누락** 칸 점유 은혜(화산 일격 등)에 회피 미적용 → 쌍검 첫 신에서 1위. 그 외 전설 이유·무익한 교체 문구·조사(을/를·과/와)·완성 대표 융합 정렬. 가중치: ALWAYS_TAKE 9→7.5, 진전 2/6→1.5/4, 은혜 단계 POOL 0. 시나리오 S17~S22 추가. 22/22 PASS. 다음: 3-A 페이블
- 2026-09-09 — 3-A 완료 (페이블): ui/SPEC.md. 핵심 결정 — 은혜 선택은 2개 찍으면 즉시 추천(버튼 없음), 결정 버튼은 카드 우하단(엄지), 하단 2×2 행동 버튼에 "은혜 선택"을 오른쪽 위, 다크 고정, 모든 변경 즉시 localStorage, 되돌리기 10단계, 폰↔PC는 텍스트 내보내기(Artifact 다운로드 불가). 다음: 3-B 오퍼스
- 2026-09-09 — 3-B 완료 (오퍼스): app/build.js + app/src/{app.css,app.js} → app/index.html 243KB 단일 파일(데이터 163 + 엔진 40 + UI 39). 375x812 검증: 가로 스크롤 0, 44px 미만 탭 타겟 0, 새로고침 시 화면 복원, S05 순위 일치(천상 기예>정전기 충격>천상 일격), engine/test.js 22/22 유지. 구현 중 수정 3건 — 신 그리드 선택 표시 미갱신, 홈 슬롯 글자 잘림(신 색 + 2줄), 다른 양상 전용 망치가 목록에 그대로 노출. Artifact 배포 + README 기록
- 2026-09-10 — 4-A 1차 (페이블): 실플레이 피드백 5건 접수. 아르카나 재설계 — 고정 세트를 자르던 방식 → 우선순위+무기 방향 힌트+각성 고정점 탐욕 선택(`recommendArcana`, DESIGN §14). 0 이해도 6장에 구조화 각성 조건(`awaken`) 추가, 이해도는 1단위(10~30) 확정. S23/S24 추가. UI 5건(글자 크기·스텝퍼·획득 경로·은혜 트레이·신 테두리)은 4-B 명세로 오퍼스에 이관
- 2026-09-10 — 4-A 1차 보정 (페이블): 아르카나 탐욕+효율 방식이 죽음(4)을 밀어내는 결함 발견(S01/S23 실패) → 핵심 기하 가중치 + 0/1 배낭 DP로 교체. 24/24 PASS. 이해도 10=기존 세트(+왕비 각성), 20=무기별 분화 확인. 엔진·데이터 완료, UI 5건은 4-B 대기
- 2026-09-10 — 4-B 완료 (오퍼스): UI 5건 구현·검증. U1 루트 글자 vw 비례(clamp 15~22px) + px→em 전환 + 사용자 배율 4단계(100/115/130/150%). U2 이해도 스텝퍼 1단위(10~30)를 런 시작·관리 양쪽에, 아르카나에 핵심/무기 방향/각성·무료 태그 + "이해도를 더 올리면" 접힘. U3 기념품·비술 획득 경로. U4 추천을 하단 트레이 안으로(컴팩트 3행, 접기 가능) — 목록 스크롤 중에도 순위가 늘 보임. U5 신 색 테두리 3px + 글로우 + 틴트 + 선택 순서 ①②③. 검증: 이해도 10→5장+왕비 각성, 20→무기별 분화(지팡이 영겁·밤 / 쌍검 사냥꾼·밤 / 도끼 영겁·사냥꾼+신성 각성), 가로 스크롤 0, 44px 미만 탭 타겟 0, 24/24 PASS. 재배포 대기
- 2026-09-10 — 4-B 추가 (오퍼스): 1·2위 점수차 < 2면 '비슷함 · 취향' 칩 표시(은혜·망치·신 전부). 근거는 시나리오 20개 점수차 분포가 0~2.5와 8 이상으로 갈리고 그 사이가 비어 있다는 것 — 2 미만은 순위 차이만 남은 구간이라 엔진이 실질 구분을 못 한다. 임계값은 app.js의 CLOSE_GAP. 칩 때문에 이름이 잘려 컴팩트 행 이름줄을 flex-wrap 허용으로 수정. 체력 3단계는 사용자 확인 후 현행 유지(ISSUES 기록)
- 2026-09-10 — 4-B 추가 2 (오퍼스): 신 13 + 무기 4 심볼을 인라인 SVG로 추가(외부 리소스 0 유지, currentColor라 신 색 그대로). 신 그리드의 색 점을 대체하고 은혜 선택 신 탭·무기 카드·홈 상단에 적용 → 색만으로 구분하던 것을 모양+색으로. 색약 대응도 됨. 도끼·횃불은 처음 그린 선 아이콘이 P자/물음표로 읽혀 채운 도형으로 다시 그림. 375px 기준 무기명 줄바꿈 없음, 가로 스크롤 0
